% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_sim.R
\name{validate_recipe}
\alias{validate_recipe}
\title{Validate a simulation recipe (schema + parameter checks)}
\usage{
validate_recipe(recipe)
}
\arguments{
\item{recipe}{A list, or a YAML/JSON file path describing the simulation.}
}
\value{
The validated (possibly augmented) recipe list; errors on failure.
}
\description{
Ensures a user-supplied recipe contains all required sections
and that distribution parameters are valid and compatible.
}
\details{
\strong{Required blocks}
\itemize{
\item \code{n} (positive number of subjects)
\item \code{covariates.defs} (list of covariate definitions)
\item \code{event_time} (AFT lognormal/Weibull or Cox exponential/piecewise-exp)
\item \code{censoring} (target overall or explicit)
\item \code{treatment} (randomization, stratified, or logistic_ps)
}

\strong{Supported models}
\itemize{
\item \code{event_time$model} in {"aft_lognormal","aft_weibull","cox_exp","cox_pwexp"}
}
}
\examples{
rec <- list(
  n = 50,
  covariates = list(defs = list(
    list(name="age", type="continuous", dist="normal", params=list(mean=60, sd=10)),
    list(name="gender", type="categorical", dist="bernoulli", params=list(p=0.4))
  )),
  event_time = list(
    model="aft_lognormal", baseline=list(mu=3, sigma=0.7),
    effects=list(treatment=-0.3, covariates=list(age=0.01, gender=-0.1)),
    tau=24
  ),
  censoring = list(mode="target_overall", target=0.25, admin_time=30),
  treatment = list(assignment="randomization", allocation="1:1")
)
validate_recipe(rec)
}
