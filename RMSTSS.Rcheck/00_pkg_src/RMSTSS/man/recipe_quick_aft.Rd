% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_convenience.R
\name{recipe_quick_aft}
\alias{recipe_quick_aft}
\title{Quick AFT recipe builder}
\usage{
recipe_quick_aft(
  n,
  tau,
  model = c("aft_lognormal", "aft_weibull"),
  baseline,
  treat_effect = 0,
  covariates,
  target_censoring = 0.25,
  allocation = "1:1",
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{tau}{RMST truncation time.}

\item{model}{Either \code{"aft_lognormal"} or \code{"aft_weibull"}.}

\item{baseline}{Named list of baseline params. For log-normal: \code{list(mu, sigma)};
for Weibull: \code{list(shape, scale)}.}

\item{treat_effect}{Numeric treatment coefficient on the linear predictor.}

\item{covariates}{List of covariate definitions (see examples).}

\item{target_censoring}{Overall censoring fraction (0–1).}

\item{allocation}{Randomization allocation string, e.g., \code{"1:1"}.}

\item{seed}{Optional RNG seed.}
}
\value{
A recipe list suitable for \\link[=simulate_from_recipe]{simulate_from_recipe}.
}
\description{
Construct a minimal recipe for an AFT model with log-normal or Weibull
baseline, randomization, and target censoring.
}
\examples{
covs <- list(
  list(name="age", type="continuous", dist="normal", params=list(mean=62, sd=10),
       transform=c("center(60)","scale(10)")),
  list(name="gender", type="categorical", dist="bernoulli", params=list(p=0.45))
)
rec <- recipe_quick_aft(n=300, tau=24, model="aft_weibull",
                        baseline=list(shape=1.3, scale=12),
                        treat_effect=-0.25, covariates=covs,
                        target_censoring=0.25, allocation="1:1", seed=2025)
dat <- simulate_from_recipe(rec)
}
