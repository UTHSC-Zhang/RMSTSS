% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_convenience.R
\name{recipe_grid}
\alias{recipe_grid}
\title{Build a grid of recipes by varying parameters}
\usage{
recipe_grid(base_recipe, vary)
}
\arguments{
\item{base_recipe}{A valid recipe list.}

\item{vary}{Named list; names are dotted paths (e.g., \code{"n"}, \code{"censoring.target"},
\code{"event_time.effects.treatment"}), values are vectors to grid over.}
}
\value{
A list of recipe lists.
}
\description{
Takes a base recipe and returns a list of recipes where named
fields are varied over supplied vectors (Cartesian product).
}
\examples{
base <- recipe_quick_aft(200, 24, "aft_lognormal",
  baseline = list(mu=3, sigma=0.7),
  treat_effect = -0.2,
  covariates = list(list(name="x", type="continuous", dist="normal", params=list(mean=0, sd=1))),
  target_censoring = 0.25
)
gs <- recipe_grid(base, list(n = c(125, 250), "censoring.target" = c(0.2, 0.3)))
length(gs)
}
