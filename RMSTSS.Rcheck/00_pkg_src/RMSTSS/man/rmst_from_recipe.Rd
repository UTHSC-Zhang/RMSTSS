% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_convenience.R
\name{rmst_from_recipe}
\alias{rmst_from_recipe}
\title{Simulate from a recipe and run an analysis function}
\usage{
rmst_from_recipe(recipe, analyze_fn, ..., n_override = NULL, seed = NULL)
}
\arguments{
\item{recipe}{A recipe list or path.}

\item{analyze_fn}{A function with signature like \verb{function(data, tau, ...)}.}

\item{...}{Additional arguments forwarded to \code{analyze_fn}.}

\item{n_override}{Optional integer, overrides \code{n} in \code{recipe}.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with elements \code{data} and \code{result}.
}
\description{
A thin convenience wrapper that generates a dataset and immediately
passes it to an analysis function (e.g., your RMST estimator).
}
\examples{
\dontrun{
# Suppose your package provides `rmst_fit(data, tau, ...)`
res <- rmst_from_recipe(
  recipe_quick_aft(200, 24, "aft_lognormal",
                   baseline = list(mu=3, sigma=0.7),
                   treat_effect = -0.2,
                   covariates = list(list(name="x", type="continuous",
                                          dist="normal", params=list(mean=0, sd=1)))),
  analyze_fn = rmst_fit
)
}
}
